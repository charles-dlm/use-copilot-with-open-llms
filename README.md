# use-copilot-with-open-llms
Connect GitHub Copilot Chat in VS Code to free NVIDIA-hosted open-source LLMs using OpenAI-compatible endpoints. This project shows how to configure custom models (e.g. DeepSeek, GLM, GPT OSS) via NVIDIA Build to replace Copilot Pro with free API access for coding and development workflows.
